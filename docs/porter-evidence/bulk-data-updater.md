# Bulk Data Updater — port report

**Upstream:** `rappen/BulkDataUpdater@40158af2445f55d0b9a32f83a916ea138f1f1196`
**Output:** `src/Plugins/BulkDataUpdater/`
**Confidence:** LOW

## Summary

3 files emitted (`*.csproj`, `PluginDescription.cs`, `BulkDataUpdaterControl.cs`).
The plugin entry class is faithful and complete. The main control surface is a
placeholder card — the original 1,099-line `BDU.cs` (+ ~700-line
`BDU.Designer.cs` not in repo because it's auto-generated by VS) cannot be
mechanically translated by a deterministic porter. The 6 dialog forms are NOT
emitted — they will follow once the parent control is hand-translated and we
know which ones are still needed.

## Confidence inputs (from inventory)

- 37 `.cs` files, 7 `.Designer.cs`, 8 `.resx`, 2 `.csproj`
- 3 `DataGridView` usages — every one will need DataGrid + ItemTemplate work
- 5 `BackgroundWorker`-style consumers via `WorkAsync(...)` on the legacy base
- 0 `OnPaint`, 0 `DllImport`, 0 Registry — no platform escapes ✅
- Hard third-party deps:
  - `RappPluginControlBase` (Rappen.XTB.Helpers, closed-source NuGet)
  - `ScintillaNET` (custom-drawn editor — Avalonia equivalent is AvaloniaEdit)
  - `Rappen.XRM.Helpers` / `Rappen.XTB.Helpers` (closed-source NuGet)
  - `XrmToolBox.Constants` (legacy shell-side constants, not in macOS shell yet)
  - `Microsoft.Xrm.Sdk.Metadata` (available — same NuGet)
  - `McTools.Xrm.Connection` (replaced by macOS `ConnectionDetail`)

## Per-file findings

| Source file | Lines | Status | Notes |
|---|---:|---|---|
| `PluginDescription.cs` | 93 | ported | All metadata preserved; image base64 stripped; `IPayPalPlugin` added to extensibility-core. |
| `BDU.cs` | 1099 | placeholder | Massive. `RappPluginControlBase`, `WorkAsync`, every UI binding lost. |
| `BDU.Designer.cs` | (generated) | not emitted | Designer-generated by VS, not in upstream tree. |
| `Forms/About.cs` + Designer | 78 + 272 | not emitted | TODO_PORT_DESIGNER. |
| `Forms/AttributeOptions.cs` + Designer | 68 + 282 | not emitted | TODO_PORT_DESIGNER. |
| `Forms/Execute.cs` + Designer | 199 + 527 | not emitted | TODO_PORT_DESIGNER. Largest dialog. |
| `Forms/SelectViewDialog.cs` + Designer | 153 + 232 | not emitted | TODO_PORT_DESIGNER. |
| `Forms/UpdateAttribute.cs` + Designer | 633 + 539 | not emitted | TODO_PORT_DESIGNER. Hairy editor. |
| `Forms/XmlContentDisplayDialog.cs` + Designer | 109 + 164 | not emitted | TODO_PORT_DESIGNER. |
| `AppCode/BDUAssign.cs` | 145 | not emitted | Pure-logic helper; eligible for direct port once base wired. |
| `AppCode/BDUDelete.cs` | 110 | not emitted | Same. |
| `AppCode/BDUEntity.cs` | 26 | not emitted | Same. |
| `AppCode/BDUGeneral.cs` | 365 | not emitted | Same. |
| `AppCode/BDUInterfaces.cs` | 79 | not emitted | Same. |
| `AppCode/BDUJob.cs` | 119 | not emitted | Same. |
| `AppCode/BDULog.cs` | 301 | not emitted | Same. |
| `AppCode/BDUSetState.cs` | 285 | not emitted | Same. |
| `AppCode/BDUUpdate.cs` | 327 | not emitted | Same. |
| `AppCode/BulkActionItem.cs` | 93 | not emitted | Same. |
| `AppCode/FindTextHandler.cs` | 66 | not emitted | Same. |
| `AppCode/Prompt.cs` | 33 | not emitted | Same. |
| `AppCode/ScintillaInitialize.cs` | 108 | not emitted | Tied to ScintillaNET — needs AvaloniaEdit. |
| `AppCode/ViewItem.cs` | 56 | not emitted | Same. |
| `AppCode/XmlFragmentWriter.cs` | 22 | not emitted | Same. |
| `Const.cs`, `GlobalSettings.cs`, `Settings.cs` | 132 (sum) | not emitted | Settings persistence has no macOS counterpart yet. |
| `Properties/AssemblyInfo.cs` | — | not emitted | Replaced by SDK-style csproj. |

## Suggested next steps for the reviewer / agentic phase

1. Hand-translate `BDU.Designer.cs` (the auto-generated layout, restored from a
   VS build) into a single `.axaml` for the main control. Use the on-screen
   sketch from spec 002 for the toolbar pattern.
2. Port the 13 `AppCode/*.cs` files verbatim (they are pure CRM-SDK code, no
   WinForms references) once the parent control compiles.
3. Replace `RappPluginControlBase`'s `WorkAsync` orchestration with
   `Task.Run(...) + Dispatcher.UIThread.Post(...)` per spec 002's type map.
4. Replace `ScintillaNET` with [`AvaloniaEdit`](https://github.com/AvaloniaUI/AvaloniaEdit) for the FetchXML / SQL panes.
5. Re-author the 6 `Forms/*` dialogs as Avalonia `Window`s (only as needed —
   `UpdateAttribute` and `Execute` are the load-bearing ones; `About` is
   replaced by the shell's About pane).
