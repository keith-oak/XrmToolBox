<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:XrmToolBox.MacOS.ViewModels"
        xmlns:plg="using:XrmToolBox.MacOS.Plugins"
        mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="720"
        x:Class="XrmToolBox.MacOS.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="XrmToolBox"
        Width="1100" Height="720"
        MinWidth="900" MinHeight="600"
        Background="{DynamicResource WindowBackgroundBrush}"
        TransparencyLevelHint="AcrylicBlur,Mica,Blur"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaTitleBarHeightHint="-1"
        ExtendClientAreaChromeHints="PreferSystemChrome">

  <Grid RowDefinitions="Auto,*,Auto">

    <!-- Toolbar — extends under the traffic lights on macOS -->
    <Border Grid.Row="0" Classes="toolbar" Padding="80,8,12,8">
      <StackPanel Orientation="Horizontal" Spacing="{DynamicResource SpacingSm}" VerticalAlignment="Center">
        <TextBlock Text="Dataverse" Classes="footnote secondary" VerticalAlignment="Center" />
        <TextBox Text="{Binding DataverseUrl}" Width="380" />
        <Button Content="{Binding ConnectButtonCaption}"
                Command="{Binding ConnectCommand}"
                Classes="primary"
                Classes.connected="{Binding IsConnected}" />
        <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="6,0,0,0"
                 IsVisible="{Binding IsConnected}"
                 Fill="{DynamicResource SuccessBrush}" />
        <TextBlock Text="{Binding ConnectionStatus}" Classes="footnote" VerticalAlignment="Center" Margin="6,0,0,0" />
      </StackPanel>
    </Border>

    <!-- Body: sidebar + tab host -->
    <Grid Grid.Row="1" ColumnDefinitions="260,1,*">

      <Border Grid.Column="0" Classes="sidebar" BorderThickness="0">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top"
                     Text="Tools"
                     Classes="footnote secondary"
                     Padding="16,12,16,4" />
          <ListBox Classes="sidebar"
                   ItemsSource="{Binding AvailablePlugins}"
                   SelectedItem="{Binding SelectedPlugin}">
            <ListBox.ItemTemplate>
              <DataTemplate DataType="plg:PluginEntry">
                <StackPanel Spacing="{DynamicResource SpacingXs}">
                  <TextBlock Text="{Binding Metadata.Name}" FontWeight="SemiBold" />
                  <TextBlock Text="{Binding Metadata.Description}"
                             Classes="footnote"
                             TextWrapping="Wrap"
                             MaxLines="2" />
                </StackPanel>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>
      </Border>

      <GridSplitter Grid.Column="1"
                    Background="{DynamicResource HairlineBrush}"
                    Width="1"
                    ResizeDirection="Columns" />

      <Grid Grid.Column="2">

        <!-- Empty state shown when no plugin is open -->
        <Border IsVisible="{Binding !OpenedPlugins.Count}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
          <StackPanel Spacing="{DynamicResource SpacingMd}"
                      HorizontalAlignment="Center"
                      MaxWidth="420">
            <TextBlock Classes="large-title secondary" Text="No tool open"
                       HorizontalAlignment="Center" />
            <TextBlock Classes="body secondary"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Text="Choose a tool from the sidebar to get started. Tools query and edit your Dataverse environment using the connection set above." />
            <TextBlock Classes="footnote secondary"
                       TextAlignment="Center"
                       IsVisible="{Binding !IsConnected}"
                       Text="Tip: connect to a Dataverse environment first." />
          </StackPanel>
        </Border>

        <!-- Tab host shown when at least one plugin is open -->
        <TabControl Classes="macos"
                    Margin="12,8,12,8"
                    IsVisible="{Binding OpenedPlugins.Count}"
                    ItemsSource="{Binding OpenedPlugins}"
                    SelectedItem="{Binding ActivePlugin}">
          <TabControl.ItemTemplate>
            <DataTemplate DataType="vm:OpenedPluginViewModel">
              <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" />
                <Button Content="✕"
                        Classes="tab-close"
                        Command="{Binding $parent[TabControl].((vm:MainWindowViewModel)DataContext).CloseTabCommand}"
                        CommandParameter="{Binding}" />
              </StackPanel>
            </DataTemplate>
          </TabControl.ItemTemplate>
          <TabControl.ContentTemplate>
            <DataTemplate DataType="vm:OpenedPluginViewModel">
              <Border Classes="card" Background="{DynamicResource SurfaceBackgroundBrush}">
                <ContentControl Content="{Binding View}" />
              </Border>
            </DataTemplate>
          </TabControl.ContentTemplate>
        </TabControl>

      </Grid>

    </Grid>

    <!-- Status bar -->
    <Border Grid.Row="2" Classes="statusbar">
      <Grid ColumnDefinitions="*,Auto">
        <TextBlock Grid.Column="0" Classes="caption mono" VerticalAlignment="Center">
          XrmToolBox · native build
        </TextBlock>
        <TextBlock Grid.Column="1" Classes="caption secondary mono" VerticalAlignment="Center"
                   Text="{Binding OpenedPlugins.Count, StringFormat='{}{0} tab(s) open'}" />
      </Grid>
    </Border>

  </Grid>

</Window>
